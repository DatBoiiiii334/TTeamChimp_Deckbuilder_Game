using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testscript : MonoBehaviour
{
    // Renderer rend;
    // public int
    
    
    //  xRange, yRange;
    // int offsetX, offsetY;

    // // maak per animatie een material aan
    // public Material idleMat;

    // // maak per state de animaties actief
    // public int state = 0;

    // // Start is called before the first frame update
    // void Start()
    // {
    //     rend = GetComponent<Renderer>();
    // }

    // Update is called once per frame
    // void Update()
    // {
    //     offsetX++;
    //     if (offsetX > xRange)
    //     { 
    //         offsetX = 0;
    //         offsetY++;
    //         if (offsetY > yRange) offsetY = 0;
    //     }

    //     // wanneer je switched van state zet de offsets op 0
        
    //     switch(state)
    //     {
    //         case 0:
    //             rend.material = idleMat;
    //             rend.material.SetTextureOffset("_MainTex", new Vector2(offsetX, offsetY)); // zoek juiste tag voor maintex volgens mij basemap
    //             rend.material.SetTextureOffset("_Tiling", new Vector2(1f / xRange, 1f / yRange)); // zoekjuiste tag voor tiling 
    //         break;
    //     }
    // }
}
